<Window x:Class="DiffGenerator2.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DiffChecker2" Height="150" Width="400" MinWidth="400" MinHeight="150" MaxHeight="150">
    <Grid Margin="10,10,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        
        <Grid Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" 
                       Text="Pasirinkite Excel failą:"/>

            <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" FontStyle="Italic" 
                       TextTrimming="CharacterEllipsis" Text="{Binding Path=Model.ExcelFileName}"/>
            <Button Grid.Column="2" Content="Pasirinkti" Width="Auto" Command="{Binding Path=Model.SelectExcelFileCommand}"/>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left"
                       Text="Pasirinkite Eip failą:"/>

            <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" FontStyle="Italic" 
                       TextTrimming="CharacterEllipsis" Text="{Binding Path=Model.EipFileName}"/>
            <Button Grid.Column="2" Content="Pasirinkti" Width="Auto" Command="{Binding Path=Model.SelectEipFileCommand}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0"/>
            <Button  Grid.Column="1" Content="Vykdyti" Command="{Binding Path=Model.ExecuteCommand}"
                     IsEnabled="{Binding Path=Model.ExecuteEnabled}"/>
            <TextBlock Grid.Column="0"/>
        </Grid>
        

    </Grid>
</Window>
